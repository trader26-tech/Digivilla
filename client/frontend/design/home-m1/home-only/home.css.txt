<style>
  /* ---- tokens (Nocturne) ---- */
  :root {
    --color-bg: #161826;
    --color-surface: #1e2130;
    --color-text: #e9e9ed;
    --color-neutral-200: #e4e7f5;
    --color-neutral-300: #c9c6da;
    --color-neutral-400: #9a9aa5;
    --color-neutral-500: #6b6e79;
    --color-neutral-600: #75798c;
    --color-neutral-700: #3f424d;
    --color-neutral-800: #2b2e3a;
    --color-neutral-900: #232634;
    --color-neutral-950: #12141d;
    --color-accent: #9184d9;
    --color-accent-100: #efedfc;
    --color-accent-200: #d2cefd;
    --color-accent-300: #b5abfc;
    --color-accent-400: #b5abfc;
    --color-accent-500: #968ae0;
    --color-accent-600: #796cbf;
    --color-accent-700: #5d5294;
    --color-accent-800: #2b2741;
    --color-accent-900: #1f1c30;
    --color-divider: #2b2e3a;
    --font-heading: Inter, system-ui, sans-serif;
    --font-heading-weight: 500;
    --font-body: Inter, system-ui, sans-serif;
    --space-2: 4px; --space-3: 6px; --space-4: 10px; --space-6: 14px; --space-8: 20px;
    --radius-sm: 5px; --radius-md: 8px;
    --shadow-sm: 0 1px 2px rgba(0,0,0,.4), 0 2px 8px rgba(0,0,0,.3);
    --shadow-lg: 0 8px 32px rgba(0,0,0,.55);
  }

  * { box-sizing: border-box; }
  body {
    margin: 0; background: var(--color-bg); color: var(--color-text);
    font-family: var(--font-body); -webkit-font-smoothing: antialiased;
  }
  a { color: var(--color-accent-300); }
  a:hover { color: var(--color-accent-200); }

  /* ---- phone shell (drop this if you're mounting in your own frame) ---- */
  .phone {
    position: relative; width: 402px; height: 874px; margin: 32px auto;
    border-radius: 44px; overflow: hidden; background: var(--color-bg);
    box-shadow: var(--shadow-lg);
    display: flex; flex-direction: column;
  }

  /* ---- HUD pills ---- */
  .one-toast { position: absolute; left: 22px; right: 22px; bottom: 96px; z-index: 50; padding: 10px 14px; border-radius: var(--radius-md);
               background: var(--color-surface); box-shadow: var(--shadow-lg), 0 0 0 1px var(--color-neutral-800); font-size: 12px; text-align: center;
               color: var(--color-neutral-200); opacity: 0; transform: translateY(8px); pointer-events: none; transition: opacity .25s, transform .25s; }
  .one-toast.show { opacity: 1; transform: none; }
  /* ---- explore: villa carousel ---- */
  .vseg { position: absolute; top: 56px; left: 22px; right: 22px; z-index: 6; display: flex; padding: 2px; border-radius: 999px;
          background: var(--color-neutral-900); box-shadow: inset 0 0 0 1px var(--color-neutral-800); }
  .vseg > span:not(.vseg-pill) { position: relative; z-index: 1; flex: 1; text-align: center; padding: 6px 0; font-size: 11px; letter-spacing: .02em; color: var(--color-neutral-400); cursor: pointer; transition: color .3s; }
  .vseg > span.on { color: var(--color-text); }
  .vseg-pill { position: absolute; top: 2px; bottom: 2px; left: 2px; width: calc((100% - 4px) / 3); border-radius: 999px;
               background: var(--color-neutral-800); box-shadow: inset 0 0 0 1px var(--color-neutral-700); transition: transform .45s cubic-bezier(.2,.8,.2,1); }
  .vstack { position: absolute; left: 0; right: 0; top: 92px; bottom: 0; overflow: hidden; touch-action: pan-y; }
  .vtrack { position: absolute; top: 0; bottom: 0; left: 0; width: 300%; display: flex; transition: transform .5s cubic-bezier(.2,.8,.2,1); will-change: transform; }
  .vcard { position: relative; flex: 0 0 calc(100% / 3); width: calc(100% / 3); min-width: 0; box-sizing: border-box; padding: 0 22px 12px; display: flex; flex-direction: column; align-items: center; overflow-y: auto; overflow-x: hidden; scrollbar-width: none; }
  .vcard::-webkit-scrollbar { display: none; }
  .vcard-glow { position: absolute; top: -30px; left: 50%; width: 340px; height: 260px; margin-left: -170px; pointer-events: none;
                background: radial-gradient(circle, oklch(0.74 0.125 150 / 0.24) 0%, transparent 65%); }
  .vcard-art { position: relative; flex: none; height: 168px; width: 100%; display: flex; align-items: center; justify-content: center; animation: exBob 6s ease-in-out infinite; }
  .vcard-art svg { height: 100%; width: auto; display: block; }
  .vcard-title { font-family: var(--font-heading); font-size: 24px; font-weight: 500; letter-spacing: -.015em; margin-top: 2px; }
  .lvl-pill { margin-top: 8px; display: flex; align-items: center; gap: 12px; padding: 5px 14px 5px 6px; border-radius: 999px;
              background: linear-gradient(180deg, var(--color-accent-700), var(--color-accent-900)); box-shadow: inset 0 1px 0 rgba(233,233,237,.14), 0 0 0 1px var(--color-accent-800), 0 6px 18px rgba(0,0,0,.35); }
  .lvl-pill .mix { font-size: 12px; font-weight: 500; letter-spacing: .06em; text-transform: uppercase; color: var(--color-accent-100); padding-left: 6px; }
  .lvl-pill .price { display: flex; align-items: center; gap: 6px; font-size: 14px; font-weight: 500; letter-spacing: -.01em; color: #f6e2a4; font-variant-numeric: tabular-nums; }
  .lvl-pill .price i { width: 16px; height: 16px; border-radius: 50%; display: grid; place-items: center; font-style: normal; font-size: 9px; font-weight: 500; color: #6a4108;
                       background: radial-gradient(circle at 34% 28%, #ffe9a3 0%, #f6c445 38%, #dc9a1f 72%, #a86b12 100%); box-shadow: inset 0 -1px 2px rgba(120,70,0,.5), 0 0 0 1px #8d5a10; }
  .vcard-sub { margin-top: 8px; font-size: 12px; text-align: center; color: var(--color-neutral-300); }
  .vcard-k { margin-top: 10px; font-size: 9px; letter-spacing: .16em; color: var(--color-neutral-400); }
  .fgrid { width: 100%; display: grid; gap: 6px; grid-template-columns: repeat(6, 1fr); margin-top: 8px; }
  .fgrid.n5 .fcard { grid-column: span 2; }
  .fgrid.n5 .fcard:nth-child(4) { grid-column: 2 / span 2; }
  .fgrid.n5 .fcard:nth-child(5) { grid-column: 4 / span 2; }
  .fgrid.n4 .fcard { grid-column: span 3; }
  .fcard { display: flex; flex-direction: column; align-items: center; gap: 3px; padding: 8px 6px 7px; min-width: 0; border-radius: 12px;
           background: linear-gradient(180deg, var(--color-neutral-900), var(--color-neutral-950, #12141d)); box-shadow: inset 0 0 0 1px var(--color-neutral-800), var(--shadow-sm); }
  .fn { font-size: 8px; letter-spacing: .1em; color: var(--color-neutral-400); white-space: nowrap; margin-top: 2px; }
  .fa { font-size: 13px; font-weight: 500; letter-spacing: -.01em; font-variant-numeric: tabular-nums; white-space: nowrap; }
  .fb { width: 100%; display: flex; align-items: center; gap: 6px; margin-top: 4px; }
  .fb i { flex: 1; height: 3px; border-radius: 2px; background: var(--color-neutral-800); position: relative; overflow: hidden; }
  .fb i b { position: absolute; left: 0; top: 0; bottom: 0; border-radius: 2px; }
  .fb em { font-style: normal; font-size: 9px; color: var(--color-neutral-500); font-variant-numeric: tabular-nums; }
  .fi { width: 36px; height: 36px; display: grid; place-items: center; position: relative; }
  .fi.vault::before { content: ''; width: 28px; height: 24px; border-radius: 8px; background: linear-gradient(180deg, #a3bcae, #6f8a7d); box-shadow: inset 0 -3px 5px rgba(0,0,0,.35), 0 0 0 1px #4f6a5c; }
  .fi.vault i { position: absolute; width: 12px; height: 12px; border-radius: 50%; border: 3px solid #2f4a3c; background: #d7e6dc; }
  .fi.coin i { display: block; width: 10px; height: 24px; border-radius: 4px; transform: rotate(18deg); background: linear-gradient(90deg, #a86b12, #f6c445 40%, #ffe9a3 55%, #dc9a1f); box-shadow: 0 0 0 1px #8d5a10, 0 4px 8px rgba(0,0,0,.4); }
  .fi.cap::after { content: ''; position: absolute; bottom: 4px; width: 20px; height: 8px; border-radius: 3px 3px 5px 5px; background: linear-gradient(180deg, #8a5a33, #5c3a1c); }
  .fi.cap i { display: block; border-radius: 50%; background: radial-gradient(circle at 35% 30%, #cfeec4, var(--c) 60%); box-shadow: inset 0 -4px 6px rgba(0,0,0,.28); position: relative; z-index: 1; }
  .fi.cap i.L { width: 20px; height: 20px; margin-bottom: 9px; }
  .fi.cap i.M { width: 15px; height: 15px; margin-bottom: 11px; }
  .fi.cap i.S { width: 11px; height: 11px; margin-bottom: 12px; }
  .fi.cap i::after { content: ''; position: absolute; left: 50%; top: 100%; width: 3px; height: 8px; margin-left: -1.5px; background: #5c8a3a; }
  .vcard-worth { width: 100%; display: flex; align-items: baseline; gap: 8px; margin-top: 10px; padding-top: 8px;
                 border-top: 1px solid transparent; border-image: linear-gradient(90deg, transparent, var(--color-neutral-800) 20%, var(--color-neutral-800) 80%, transparent) 1; }
  .vcard-worth span { font-size: 11px; color: var(--color-neutral-400); }
  .vcard-worth b { font-weight: 500; font-size: 20px; letter-spacing: -.015em; font-variant-numeric: tabular-nums; }
  .vcard-worth small { margin-left: auto; font-size: 10px; color: var(--color-neutral-500); white-space: nowrap; }
  @keyframes exBob { 0%,100% { transform: translateY(0) } 50% { transform: translateY(-5px) } }

  /* ---- inflow / outflow header ---- */
  .flows { position: absolute; top: 58px; left: 22px; right: 22px; z-index: 30; display: flex; align-items: stretch; }
  .flows[hidden] { display: none; }
  .flow { flex: 1; min-width: 0; padding: 0; cursor: pointer; display: flex; flex-direction: column; gap: 4px; }
  .flow.out { align-items: flex-end; text-align: right; }
  .flow:hover .flow-fig { filter: brightness(1.12); }
  .flow-div { width: 1px; margin: 4px 0; background: linear-gradient(180deg, transparent, rgba(233,233,237,.14), transparent); }
  .flow-head { display: flex; align-items: center; gap: 6px; }
  .flow-lbl { font-size: 9px; letter-spacing: .14em; color: var(--color-neutral-400); }
  .flow-arrow { width: 16px; height: 16px; border-radius: 50%; display: flex; align-items: center; justify-content: center;
                font-size: 11px; line-height: 1; }
  .flow.in .flow-arrow { color: #6b5111; background: radial-gradient(circle at 34% 28%, #ffe9a3 0%, #f6c445 38%, #dc9a1f 72%, #a86b12 100%); box-shadow: 0 0 8px rgba(246,196,69,.35); }
  .flow.out .flow-arrow { color: #2b2741; background: radial-gradient(circle at 34% 26%, #e7e5fe 0%, #b5abfc 36%, #796cbf 72%, #423a6a 100%); box-shadow: 0 0 8px rgba(181,171,252,.35); }
  .flow-fig { font-family: var(--font-heading); font-size: 24px; letter-spacing: -.015em; line-height: 1.1; font-variant-numeric: tabular-nums; }
  .flow-fig span:last-child { font-size: 11px; letter-spacing: 0; color: var(--color-neutral-500); margin-left: 2px; }
  .flow-fig.gold { color: #f3dfa8; }
  .flow-fig.violet { color: var(--color-accent-200); }
  .flow-sub { font-size: 10px; color: var(--color-neutral-500); white-space: nowrap; }
  .hud { position: absolute; top: 58px; right: 18px; z-index: 30;
         display: flex; flex-direction: column; align-items: flex-end; gap: 6px; }
  .pill { display: flex; align-items: center; cursor: pointer; height: 26px; }
  .pill .coin { position: relative; z-index: 2; width: 26px; height: 26px; border-radius: 50%;
                overflow: hidden; display: flex; align-items: center; justify-content: center;
                box-shadow: 0 2px 5px rgba(0,0,0,.45); }
  .pill .coin.gold { background: radial-gradient(circle at 34% 28%, #ffe9a3 0%, #f6c445 38%, #dc9a1f 72%, #a86b12 100%);
                     border: 1.5px solid #8d5a10; }
  .pill .coin.violet { background: radial-gradient(circle at 34% 26%, #e7e5fe 0%, #b5abfc 36%, #796cbf 72%, #423a6a 100%);
                       border: 1.5px solid #2b2741; }
  .pill .coin .face { width: 11px; height: 11px; border-radius: 3px; transform: rotate(45deg);
                      background: linear-gradient(135deg, #fff3c9, #e8b53c); }
  .pill .coin.violet .face { width: 10px; height: 13px; border-radius: 50%/62% 62% 38% 38%;
                             background: linear-gradient(160deg, #f5f4ff, #968ae0 70%, #5d5294); }
  .pill .coin .sheen { position: absolute; top: 0; left: 0; width: 10px; height: 100%;
    background: linear-gradient(90deg, rgba(255,255,255,0), rgba(255,255,255,.55), rgba(255,255,255,0));
    animation: shine 3.4s ease-in-out infinite; }
  .pill .body { margin-left: -9px; display: flex; align-items: center; justify-content: space-between;
    gap: 8px; height: 20px; min-width: 154px; width: auto; white-space: nowrap; padding: 0 10px 0 15px; border-radius: 10px;
    background: rgba(35,37,50,.92); box-shadow: inset 0 0 0 1px rgba(233,233,237,.10); }
  .pill .label { flex: none; font-size: 9px; letter-spacing: .12em; color: var(--color-neutral-400); }
  .pill .value { font-family: var(--font-heading); font-size: 13px; font-variant-numeric: tabular-nums; }
  .pill .value.gold { color: #f3dfa8; }
  .pill .value.violet { color: var(--color-accent-200); }

  /* ---- board ---- */
  .stage { position: relative; flex: 1; min-height: 0;
           padding: 150px 6px 0; display: flex; flex-direction: column;
           justify-content: space-evenly; gap: 0; }
  .glow { position: absolute; top: -40px; left: 50%; width: 340px; height: 340px;
          margin-left: -170px; border-radius: 50%; pointer-events: none;
          background: radial-gradient(circle, rgba(108,186,54,.13) 0%, transparent 66%); }
  .title { text-align: center; }
  .title .kicker { font-size: 10px; letter-spacing: .14em; color: var(--color-neutral-400); }
  .title .name { margin-top: 2px; font-family: var(--font-heading); font-size: 24px; letter-spacing: -.01em; }

  .board { position: relative; }
  .board svg { width: 100%; height: auto; display: block; margin: -10px auto -6px; }
  .cell { cursor: pointer; }
  .cell:hover { filter: brightness(1.08); }

  .rent { text-align: center; }
  .rent .row { display: flex; align-items: baseline; justify-content: center; gap: var(--space-3); }
  .rent .lbl { font-size: 11px; color: var(--color-neutral-400); }
  .rent .amt { font-family: var(--font-heading); font-size: 20px; color: var(--color-neutral-200); }
  .rent .note { margin-top: 3px; font-size: 11px; color: var(--color-neutral-500); }
  .rent .note b { font-weight: 400; color: var(--color-neutral-300); }

  .legend { display: flex; justify-content: center; gap: var(--space-8);
            font-size: 11px; color: var(--color-neutral-400); }
  .legend span { display: flex; align-items: center; gap: 6px; }
  .legend i { width: 9px; height: 9px; }
  .legend i.villa { background: #efe6d7; }
  .legend i.plot { background: #6cba36; }
  .legend i.open { border: 1px dashed var(--color-neutral-700); }

  /* ---- portfolio value + fund mix ---- */
  .port { padding: 0 16px; display: flex; flex-direction: column; align-items: center; gap: 4px; }
  .port-k { font-size: 10px; letter-spacing: .16em; color: var(--color-neutral-400); }
  .port-v { font-family: var(--font-heading); font-size: 34px; line-height: 1.05; letter-spacing: -.02em; font-variant-numeric: tabular-nums; }
  .port-m { display: flex; align-items: center; gap: 8px; font-size: 12px; color: var(--color-neutral-300); }
  .port-m i { width: 3px; height: 3px; border-radius: 50%; background: var(--color-neutral-600); }
  .port-m b { font-weight: 400; color: #8fd65a; }
  .port-bar { width: 100%; display: flex; gap: 2px; height: 5px; border-radius: 3px; overflow: hidden; margin-top: 12px; }
  .port-bar span { height: 100%; }
  .port-funds { width: 100%; display: flex; justify-content: space-between; gap: 4px; margin-top: 10px; }
  .port-funds div { flex: 1; min-width: 0; display: flex; flex-direction: column; align-items: center; gap: 3px; }
  .port-funds .a { font-size: 13px; font-weight: 500; letter-spacing: -.01em; font-variant-numeric: tabular-nums; white-space: nowrap; }
  .port-funds .n { font-size: 9px; letter-spacing: .06em; white-space: nowrap; }

  .port-ctas { margin-top: 14px; width: 100%; display: flex; gap: 8px; }
  .port-cta .lv { width: 22px; height: 22px; border-radius: 50%; display: grid; place-items: center; font-size: 12px; font-weight: 500; color: var(--color-accent-100);
                  background: linear-gradient(180deg, var(--color-accent-700), var(--color-accent-900)); box-shadow: inset 0 1px 0 rgba(233,233,237,.14); }
  .port-cta { display: flex; align-items: center; justify-content: center; gap: 8px; flex: 1; min-width: 0; padding: 11px 16px; border-radius: var(--radius-md); cursor: pointer;
              border: 1px solid var(--color-accent); color: var(--color-accent-100); background: transparent; font-size: 13px; letter-spacing: .01em; position: relative; overflow: hidden;
              transition: background .25s, transform .15s; }
  .port-cta:hover { background: var(--color-accent-900); }
  .port-cta:active { transform: scale(.985); }
  .port-cta::after { content: ''; position: absolute; top: 0; left: -40%; width: 30%; height: 100%; background: linear-gradient(90deg, transparent, rgba(145,132,217,.22), transparent); transform: skewX(-18deg); animation: ctaSheen 4.2s ease-in-out infinite; }
  .port-cta b { font-weight: 500; }
  .port-cta i { font-style: normal; color: var(--color-accent-300); transition: transform .25s; }
  .port-cta:hover i { transform: translateX(3px); }
  @keyframes ctaSheen { 0%,60% { left: -40%; } 100% { left: 120%; } }

  /* ---- portfolio page ---- */
  #scr-portfolio { overflow-y: auto; scrollbar-width: none; }
  #scr-portfolio::-webkit-scrollbar { display: none; }
  .pf { padding: 56px 22px 28px; display: flex; flex-direction: column; }
  .pf-k { font-size: 10px; letter-spacing: .16em; color: var(--color-neutral-400); }
  .pf-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; }
  .pf-v { font-family: var(--font-heading); font-size: 36px; line-height: 1.05; letter-spacing: -.025em; font-variant-numeric: tabular-nums; margin-top: 8px; }
  .pf-delta { margin-top: 8px; display: flex; align-items: center; gap: 8px; font-size: 12px; color: var(--color-neutral-400); font-variant-numeric: tabular-nums; white-space: nowrap; }
  .pf-head > div:first-child { min-width: 0; }
  .pf-delta b { font-weight: 500; color: #8fd48a; }
  .pf-delta i { width: 3px; height: 3px; border-radius: 50%; background: var(--color-neutral-600); }
  .pf-lvl { flex: none; display: flex; flex-direction: column; align-items: center; gap: 2px; padding: 8px 12px 7px; border-radius: 12px; margin-top: 4px;
            background: linear-gradient(180deg, var(--color-accent-800), var(--color-accent-900)); box-shadow: inset 0 1px 0 rgba(233,233,237,.12), 0 0 0 1px var(--color-accent-800); }
  .pf-lvl b { font-family: var(--font-heading); font-size: 20px; font-weight: 500; line-height: 1; color: var(--color-accent-100); }
  .pf-lvl span { font-size: 8px; letter-spacing: .14em; color: var(--color-accent-300); }
  .pf-chart { position: relative; margin: 14px -6px 0; height: 118px; }
  .pf-chart svg { width: 100%; height: 100%; display: block; overflow: visible; }
  .pf-chart .ln { fill: none; stroke: var(--color-accent); stroke-width: 1.6; stroke-linejoin: round; stroke-linecap: round; stroke-dasharray: 1000; stroke-dashoffset: 1000; }
  .pf-chart .ar { fill: url(#pfGrad); opacity: 0; }
  .pf-chart .inv { fill: none; stroke: var(--color-neutral-600); stroke-width: 1; stroke-dasharray: 3 4; }
  .pf-chart .dot { fill: var(--color-accent-100); stroke: var(--color-accent); stroke-width: 2; opacity: 0; transform-origin: center; transform-box: fill-box; }
  .pf.live .pf-chart .ln { animation: pfDraw 1.6s cubic-bezier(.4,0,.2,1) forwards; }
  .pf.live .pf-chart .ar { animation: pfFade .8s .9s forwards; }
  .pf.live .pf-chart .dot { animation: pfPop .5s 1.5s cubic-bezier(.2,1.4,.4,1) forwards; }
  @keyframes pfDraw { to { stroke-dashoffset: 0; } }
  @keyframes pfFade { to { opacity: 1; } }
  @keyframes pfPop { from { opacity: 0; transform: scale(0); } to { opacity: 1; transform: scale(1); } }
  .pf-axis { display: flex; justify-content: space-between; font-size: 9px; letter-spacing: .06em; color: var(--color-neutral-500); margin-top: 2px; }
  .pf-axis span:last-child { color: var(--color-neutral-300); }

  .pf-rank { margin-top: 22px; display: grid; grid-template-columns: 72px 1fr; gap: 14px; align-items: center; padding: 14px 16px; border-radius: 14px;
             background: linear-gradient(135deg, var(--color-neutral-900), var(--color-neutral-950, #12141d)); box-shadow: inset 0 0 0 1px var(--color-neutral-800), var(--shadow-sm); position: relative; overflow: hidden; }
  .pf-rank::before { content: ''; position: absolute; right: -40px; top: -60px; width: 160px; height: 160px; border-radius: 50%; background: radial-gradient(circle, rgba(145,132,217,.18), transparent 70%); pointer-events: none; }
  .pf-ring { position: relative; width: 72px; height: 72px; }
  .pf-ring svg { width: 100%; height: 100%; transform: rotate(-90deg); }
  .pf-ring .bg { fill: none; stroke: var(--color-neutral-800); stroke-width: 5; }
  .pf-ring .fg { fill: none; stroke: var(--color-accent); stroke-width: 5; stroke-linecap: round; stroke-dasharray: 201; stroke-dashoffset: 201; }
  .pf.live .pf-ring .fg { animation: pfRing 1.4s .4s cubic-bezier(.4,0,.2,1) forwards; }
  @keyframes pfRing { to { stroke-dashoffset: var(--off); } }
  .pf-ring b { position: absolute; inset: 0; display: grid; place-items: center; font-family: var(--font-heading); font-size: 15px; font-weight: 500; letter-spacing: -.02em; }
  .pf-rank h3 { margin: 0; font-size: 15px; font-weight: 500; letter-spacing: -.01em; line-height: 1.3; }
  .pf-rank h3 em { font-style: normal; color: var(--color-accent-200); }
  .pf-rank p { margin: 4px 0 0; font-size: 11px; line-height: 1.4; color: var(--color-neutral-400); }
  .pf-rank { padding: 12px 14px; }

  .pf-keys { margin-top: 12px; display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
  .pf-keys > div { padding: 12px 14px; border-radius: 12px; background: var(--color-surface); box-shadow: inset 0 0 0 1px var(--color-neutral-800); opacity: 0; transform: translateY(8px); }
  .pf.live .pf-keys > div { animation: pfUp .5s cubic-bezier(.2,.8,.2,1) forwards; animation-delay: calc(var(--i) * 60ms + .35s); }
  .pf-keys b { display: block; font-family: var(--font-heading); font-size: 20px; font-weight: 500; letter-spacing: -.02em; font-variant-numeric: tabular-nums; line-height: 1.1; }
  .pf-keys span { display: block; font-size: 11px; color: var(--color-neutral-300); margin-top: 3px; }
  .pf-keys small { display: block; font-size: 10px; color: var(--color-neutral-500); margin-top: 2px; }
  .pf-keys > div:first-child b { color: #f3dfa8; }
  .pf-ladder { margin-top: 18px; }
  .pf-ladder-h { display: flex; align-items: baseline; justify-content: space-between; }
  .pf-ladder-h span { flex: none; white-space: nowrap; font-size: 11px; color: var(--color-neutral-400); }
  .pf-ladder-h b { font-weight: 500; font-size: 11px; color: var(--color-neutral-300); font-variant-numeric: tabular-nums; white-space: nowrap; }
  .pf-ladder-h > b { color: var(--color-accent-300); font-weight: 400; }
  .pf-tiles { display: grid; grid-template-columns: repeat(9, 1fr); gap: 4px; margin-top: 8px; }
  .pf-tiles i { display: block; height: 6px; border-radius: 3px; background: var(--color-neutral-800); transform: scaleX(0); transform-origin: left; }
  .pf-tiles i.own { background: var(--color-accent); }
  .pf-tiles i.bld { background: repeating-linear-gradient(90deg, var(--color-accent-400) 0 3px, var(--color-accent-800) 3px 6px); }
  .pf.live .pf-tiles i { animation: pfTile .5s cubic-bezier(.2,.8,.2,1) forwards; animation-delay: calc(var(--i) * 60ms + .3s); }
  @keyframes pfTile { to { transform: scaleX(1); } }
  .pf-ladder-n { margin-top: 7px; font-size: 11px; color: var(--color-neutral-500); }
  .pf-ladder-n b { font-weight: 400; color: var(--color-neutral-300); }

  .pf-sec { margin-top: 26px; display: flex; align-items: baseline; justify-content: space-between; }
  .pf-sec h2 { margin: 0; font-size: 17px; font-weight: 500; letter-spacing: -.015em; }
  .pf-sec span { font-size: 11px; color: var(--color-neutral-500); }
  .pf-hold { margin-top: 10px; display: flex; flex-direction: column; gap: 8px; }
  .hd { border-radius: 14px; background: var(--color-surface); box-shadow: inset 0 0 0 1px var(--color-neutral-800), var(--shadow-sm); overflow: hidden; opacity: 0; transform: translateY(10px); }
  .pf.live .hd { animation: pfUp .55s cubic-bezier(.2,.8,.2,1) forwards; animation-delay: calc(var(--i) * 70ms + .5s); }
  @keyframes pfUp { to { opacity: 1; transform: none; } }
  .hd-top { display: grid; grid-template-columns: 54px 1fr auto; gap: 12px; align-items: center; padding: 12px 14px 12px 10px; cursor: pointer; }
  .hd-top:hover { background: var(--color-neutral-900); }
  .hd-art { width: 54px; height: 44px; display: grid; place-items: center; }
  .hd-art svg { width: 54px; height: 44px; overflow: visible; }
  .hd-name { display: flex; align-items: center; gap: 8px; font-size: 14px; font-weight: 500; letter-spacing: -.01em; }
  .hd-tag { font-size: 8px; letter-spacing: .12em; padding: 3px 7px; border-radius: 999px; background: var(--color-accent-900); color: var(--color-accent-200); box-shadow: inset 0 0 0 1px var(--color-accent-800); }
  .hd-tag.bld { background: var(--color-neutral-900); color: var(--color-neutral-300); box-shadow: inset 0 0 0 1px var(--color-neutral-700); }
  .hd-sub { margin-top: 4px; font-size: 11px; color: var(--color-neutral-500); font-variant-numeric: tabular-nums; }
  .hd-mix { margin-top: 7px; display: flex; gap: 2px; height: 3px; border-radius: 2px; overflow: hidden; }
  .hd-mix span { height: 100%; }
  .hd-val { text-align: right; font-variant-numeric: tabular-nums; }
  .hd-val b { display: block; font-weight: 500; font-size: 15px; letter-spacing: -.015em; }
  .hd-val span { display: block; font-size: 11px; color: #8fd48a; margin-top: 2px; }
  .hd-val em { display: block; font-style: normal; font-size: 10px; color: var(--color-neutral-500); margin-top: 6px; transition: transform .3s; }
  .hd.open .hd-val em { transform: rotate(180deg); }
  .hd-body { max-height: 0; overflow: hidden; transition: max-height .45s cubic-bezier(.4,0,.2,1); }
  .hd.open .hd-body { max-height: 420px; }
  .hd-funds { padding: 2px 14px 14px; display: flex; flex-direction: column; gap: 1px; border-top: 1px solid transparent; border-image: linear-gradient(90deg, transparent, var(--color-neutral-800) 15%, var(--color-neutral-800) 85%, transparent) 1; }
  .hf { display: grid; grid-template-columns: 8px 1fr 76px auto; gap: 10px; align-items: center; padding: 9px 0; }
  .hf + .hf { border-top: 1px solid var(--color-neutral-900); }
  .hf i { width: 8px; height: 8px; border-radius: 2px; }
  .hf .n { font-size: 12px; }
  .hf .n small { display: block; font-size: 10px; color: var(--color-neutral-500); margin-top: 1px; font-variant-numeric: tabular-nums; }
  .hf svg { width: 76px; height: 22px; overflow: visible; }
  .hf svg path { fill: none; stroke-width: 1.4; stroke-linecap: round; stroke-linejoin: round; }
  .hf .v { text-align: right; font-variant-numeric: tabular-nums; font-size: 12px; }
  .hf .v small { display: block; font-size: 10px; color: #8fd48a; margin-top: 1px; }
  .hd-note { padding: 0 14px 12px; font-size: 10px; line-height: 1.5; color: var(--color-neutral-500); }

  .pf-funds { margin-top: 10px; display: flex; flex-direction: column; gap: 6px; }
  .pfund { display: grid; grid-template-columns: 34px 1fr 64px; gap: 12px; align-items: center; padding: 10px 12px; border-radius: 12px; background: var(--color-surface); box-shadow: inset 0 0 0 1px var(--color-neutral-800); opacity: 0; transform: translateY(8px); }
  .pf.live .pfund { animation: pfUp .5s cubic-bezier(.2,.8,.2,1) forwards; animation-delay: calc(var(--i) * 60ms + .9s); }
  .pfund .ic { width: 34px; height: 34px; border-radius: 10px; display: grid; place-items: center; background: var(--color-neutral-900); }
  .pfund .ic i { display: block; width: 12px; height: 12px; border-radius: 50%; }
  .pfund .n { font-size: 12px; font-weight: 500; }
  .pfund .n small { display: block; font-size: 10px; color: var(--color-neutral-500); margin-top: 2px; font-variant-numeric: tabular-nums; }
  .pfund .bar { margin-top: 6px; height: 3px; border-radius: 2px; background: var(--color-neutral-800); overflow: hidden; }
  .pfund .bar i { display: block; height: 100%; border-radius: 2px; transform: scaleX(0); transform-origin: left; }
  .pf.live .pfund .bar i { animation: pfTile .9s cubic-bezier(.2,.8,.2,1) forwards; animation-delay: calc(var(--i) * 60ms + 1.2s); }
  .pfund .r { text-align: right; font-variant-numeric: tabular-nums; }
  .pfund .r b { display: block; font-weight: 500; font-size: 15px; letter-spacing: -.015em; color: #8fd48a; }
  .pfund .r small { display: block; font-size: 9px; letter-spacing: .06em; color: var(--color-neutral-500); margin-top: 2px; }
  .pf-more { margin-top: 22px; display: flex; align-items: center; justify-content: space-between; padding: 12px 14px; border-radius: 12px; cursor: pointer; font-size: 12px; color: var(--color-neutral-300);
             box-shadow: inset 0 0 0 1px var(--color-neutral-800); }
  .pf-more:hover { background: var(--color-neutral-900); }
  .pf-more i { font-style: normal; color: var(--color-accent-300); }
  .pf-fine { margin-top: 16px; font-size: 10px; line-height: 1.5; color: var(--color-neutral-500); }
  .pf-back { position: absolute; top: 56px; left: 22px; z-index: 5; font-size: 12px; color: var(--color-accent-300); cursor: pointer; }

  .fine-row { display: flex; align-items: center; gap: var(--space-4); font-size: 12px; line-height: 1.5; color: var(--color-neutral-400); }
  .fine-mini { width: 40px; height: 30px; flex: none; display: block; }
  .fine-mini.lg { width: 72px; height: 54px; }
  .fine-link { text-align: center; font-size: 11px; color: var(--color-neutral-500); cursor: pointer; }
  .fine-link span { color: var(--color-accent-300); }
  .fine-link:hover { color: var(--color-neutral-300); }
  .fine-row b { font-weight: 400; color: var(--color-neutral-300); }
  .fine-note { margin-top: 2px; text-align: center; }
  .fine { flex: none; padding: var(--space-4) 22px var(--space-6);
          font-size: 11px; line-height: 1.5; color: var(--color-neutral-500); }

  .cell.building use[href="#tGround"] { animation: groundGlow 2.8s ease-in-out infinite; }
  @keyframes groundGlow { 0%,100% { filter: drop-shadow(0 0 6px rgba(124,201,69,.35)); } 50% { filter: drop-shadow(0 0 16px rgba(124,201,69,.8)); } }
  /* ---- tab bar ---- */
  .tabs { position: absolute; left: 50%; bottom: 22px; transform: translateX(-50%); z-index: 60;
          display: flex; align-items: center; gap: 4px; padding: 6px; border-radius: 34px;
          background: rgba(30,33,48,.94); box-shadow: 0 0 0 1px var(--color-neutral-700,#2b2e3a), 0 12px 30px rgba(0,0,0,.5);
          backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px); }
  .tabs div { display: flex; align-items: center; gap: 8px; height: 46px; padding: 0 20px; border-radius: 23px;
              cursor: pointer; color: var(--color-neutral-400); transition: background .25s, padding .25s, color .25s; }
  .tabs div.on { color: var(--color-accent-300); background: #2b2741; padding: 0 22px; }
  .tabs div svg, .tabs div > span:first-child { flex: none; }
  .tabs span.lbl { display: none; font-size: 15px; font-weight: 500; letter-spacing: -.01em; }
  .tabs div.on span.lbl { display: inline; }
  .tabs div[data-tab="explore"] { display: none; }

  /* ---- modal ---- */
  .scrim { position: absolute; inset: 0; z-index: 40; display: none;
    align-items: center; justify-content: center; padding: 22px;
    background: rgba(11,13,22,.62); backdrop-filter: blur(9px); -webkit-backdrop-filter: blur(9px); }
  .scrim.open { display: flex; }
  .sheet { width: 100%; max-width: 340px; border-radius: var(--radius-md);
           background: var(--color-surface); box-shadow: var(--shadow-lg); overflow: hidden; }
  .sheet .hero { height: 118px; display: flex; align-items: center; justify-content: center;
    background: radial-gradient(circle at 50% 62%, rgba(108,186,54,.24) 0%, transparent 68%); }
  .sheet .hero svg { width: 78%; height: 100%; display: block; }
  .sheet .pad { padding: var(--space-4) var(--space-6) var(--space-6); }
  .sheet .head { display: flex; align-items: baseline; justify-content: space-between; }
  .sheet .h { font-family: var(--font-heading); font-size: 17px; }
  .sheet .close { cursor: pointer; font-size: 11px; color: var(--color-neutral-500); }
  .sheet .sub { margin-top: 6px; font-size: 12px; color: var(--color-neutral-400); }
  .tag { display: inline-block; padding: 2px 7px; border-radius: 4px; font-size: 10px;
         letter-spacing: .04em; background: var(--color-accent-800); color: var(--color-accent-200); }

  /* ---- SIP status sheet (opens from an open tile) ---- */
  .sip { position: absolute; inset: 0; z-index: 45; display: none; flex-direction: column;
         background: var(--color-bg); }
  .sip.open { display: flex; animation: sipIn .45s cubic-bezier(.2,.9,.3,1.05) both; }
  @keyframes sipIn { 0% { transform: translateY(100%); opacity: .4 } 100% { transform: translateY(0); opacity: 1 } }
  .sip-nav { display: flex; align-items: center; justify-content: space-between; padding: 58px 22px 0; }
  .sip-back { font-size: 13px; color: var(--color-accent-300); cursor: pointer; }
  .sip-kicker { font-size: 10px; letter-spacing: .14em; color: var(--color-neutral-400); }
  .sip-body { flex: 1; min-height: 0; position: relative; display: flex; flex-direction: column; }
  .sip-top { position: relative; z-index: 2; padding: 18px 22px 0; display: flex; flex-direction: column; align-items: center; gap: 8px; }
  .sip-income { display: flex; align-items: center; gap: 10px; cursor: pointer; user-select: none; }
  .sip-income .coin { width: 30px; height: 30px; }
  .sip-income .coin.dim { opacity: .45; }
  .sip-fig { font-size: 22px; font-family: var(--font-heading); letter-spacing: -.01em; color: var(--color-neutral-400); }
  .sip-fig.on { color: var(--color-text); }
  .sip-segs { display: flex; gap: 4px; margin-top: 4px; }
  .sip-segs span { width: 34px; height: 4px; border-radius: 2px; background: var(--color-neutral-800); transition: background .5s, box-shadow .5s; }
  .sip-segs span.on { background: #6cba36; box-shadow: 0 0 8px rgba(108,186,54,.5); }
  .sip-line { font-size: 11px; color: var(--color-neutral-300); }
  .sip-line b { font-weight: 400; color: #8fd65a; }
  .sip-stage { margin-top: 14px; text-align: center; }
  .sip-stage .n { font-family: var(--font-heading); font-size: 30px; letter-spacing: -.01em; }
  .sip-stage .t { margin-top: 4px; font-size: 10px; letter-spacing: .16em; color: #8fd65a; }
  .sip-hero { position: absolute; left: 0; right: 0; top: 150px; bottom: 170px; display: flex; align-items: center; justify-content: center;
              background: radial-gradient(ellipse at 50% 55%, rgba(108,186,54,.18) 0%, transparent 62%); }
  .sip-hero svg { width: 92%; height: 100%; display: block; overflow: visible; }
  .sip-hero.pop svg { animation: sipPop .9s cubic-bezier(.2,.9,.3,1.15) both; }
  @keyframes sipPop { 0% { transform: translateY(30px) scale(.92); opacity: .3 } 60% { transform: translateY(-8px) scale(1.02); opacity: 1 } 100% { transform: none } }
  .sip-bottom { position: absolute; left: 22px; right: 22px; bottom: 18px; z-index: 2; display: flex; flex-direction: column; align-items: center; gap: 6px; }
  .sip-val { font-family: var(--font-heading); font-size: 40px; letter-spacing: -.02em; font-variant-numeric: tabular-nums; }
  .sip-meta { display: flex; gap: 8px; align-items: center; font-size: 12px; color: var(--color-neutral-300); }
  .sip-meta i { width: 3px; height: 3px; border-radius: 50%; background: var(--color-neutral-600); }
  .sip-meta b { font-weight: 400; color: #8fd65a; }
  .sip-cta { margin-top: 10px; width: 100%; display: flex; align-items: center; justify-content: center; gap: 8px; height: 40px;
             border-radius: var(--radius-md); border: 1px solid var(--color-accent); color: var(--color-accent-200); font-size: 13px; cursor: pointer; }
  .sip-cta:hover { background: var(--color-accent-900); }
  .sip-cta:active { transform: scale(.98); }
  .sip-fine { font-size: 10px; line-height: 1.5; color: var(--color-neutral-500); text-align: center; }
  .sip-toast { position: absolute; top: 96px; left: 14px; right: 14px; z-index: 6; display: flex; align-items: center; gap: 12px; padding: 10px 14px 10px 12px;
               border-radius: var(--radius-md); background: var(--color-surface); box-shadow: var(--shadow-lg), 0 0 0 1px var(--color-neutral-800);
               pointer-events: none; opacity: 0; }
  .sip-toast.show { animation: sipToast 2.6s both; }
  @keyframes sipToast { 0% { transform: translateY(-120%); opacity: 0 } 12% { transform: translateY(0); opacity: 1 } 80% { transform: translateY(0); opacity: 1 } 100% { transform: translateY(-120%); opacity: 0 } }
  .sip-toast .coin { width: 30px; height: 30px; }
  .sip-toast .k { font-size: 10px; letter-spacing: .14em; color: #8fd65a; }
  .sip-toast .v { font-family: var(--font-heading); font-size: 18px; }
  .sip-toast .d { margin-left: auto; font-size: 11px; color: var(--color-neutral-400); white-space: nowrap; }
  .cell.building { filter: drop-shadow(0 0 6px rgba(108,186,54,.35)); }

  :focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; }

  /* ---- settings ---- */
  .st { flex: 1; min-height: 0; overflow-y: auto; padding: 58px 22px 24px; display: flex; flex-direction: column; gap: 22px; }
  .st > * { flex: none; }
  .st-k { font-size: 10px; letter-spacing: .14em; text-transform: uppercase; color: var(--color-neutral-400); }
  .st-id { display: flex; align-items: center; gap: 14px; }
  .st-av { position: relative; width: 56px; height: 56px; border-radius: 16px; flex: none; display: flex; align-items: center; justify-content: center;
           font-family: var(--font-heading); font-size: 24px; color: var(--color-accent-100);
           background: radial-gradient(circle at 30% 25%, var(--color-accent-500), var(--color-accent-800));
           box-shadow: 0 0 0 1px var(--color-accent-700), 0 8px 22px -8px var(--color-accent-700); }
  .st-av i { position: absolute; right: -4px; bottom: -4px; width: 20px; height: 20px; border-radius: 50%; background: var(--color-surface);
             box-shadow: 0 0 0 1px var(--color-neutral-700); display: flex; align-items: center; justify-content: center; color: var(--color-accent-300); }
  .st-name { font-family: var(--font-heading); font-size: 20px; line-height: 1.2; }
  .st-sub { font-size: 12px; color: var(--color-neutral-400); margin-top: 3px; display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
  .st-kyc { display: inline-flex; align-items: center; gap: 5px; padding: 2px 8px 2px 6px; border-radius: 999px; font-size: 10px; letter-spacing: .06em;
            background: rgba(88,184,88,.12); color: #8fd48f; }
  .st-kyc::before { content: ''; width: 6px; height: 6px; border-radius: 50%; background: #58b858; box-shadow: 0 0 0 3px rgba(88,184,88,.18); }
  .st-out { margin-left: auto; width: 36px; height: 36px; border-radius: 10px; border: 1px solid var(--color-neutral-800); background: transparent;
            color: var(--color-neutral-400); display: flex; align-items: center; justify-content: center; cursor: pointer; }
  .st-out:hover { background: var(--color-neutral-900); color: var(--color-neutral-200); }

  /* presentation: poster with a miniature of the estate and a pulsing play ring */
  .st-pres { position: relative; display: block; border-radius: 16px; overflow: hidden; height: 160px; cursor: pointer;
             background: linear-gradient(160deg, #23263a 0%, #191b2a 60%, #161826 100%); box-shadow: 0 0 0 1px var(--color-neutral-800), 0 14px 30px -14px rgba(0,0,0,.7); }
  .st-pres:hover .st-play { transform: scale(1.06); }
  .st-pres .mini { position: absolute; right: -6px; top: -8px; width: 196px; opacity: .95; filter: drop-shadow(0 18px 26px rgba(0,0,0,.5)); }
  .st-pres .glow { position: absolute; right: 30px; top: 30px; width: 200px; height: 130px; border-radius: 50%; pointer-events: none;
                   background: radial-gradient(circle, rgba(145,132,217,.28), transparent 70%); }
  .st-pres .txt { position: absolute; left: 18px; bottom: 16px; right: 150px; }
  .st-pres .txt b { display: block; font-weight: 500; font-size: 16px; line-height: 1.25; }
  .st-pres .txt span { display: block; font-size: 11px; color: var(--color-neutral-400); margin-top: 4px; }
  .st-pres .dur { position: absolute; left: 18px; top: 14px; font-size: 10px; letter-spacing: .12em; color: var(--color-accent-300); display: flex; gap: 6px; align-items: center; }
  .st-pres .dur::before { content: ''; width: 5px; height: 5px; border-radius: 50%; background: var(--color-accent); }
  .st-play { position: absolute; right: 16px; bottom: 16px; width: 44px; height: 44px; border-radius: 50%; display: flex; align-items: center; justify-content: center;
             background: var(--color-accent-100); color: #2b2741; transition: transform .25s ease; }
  .st-play::after { content: ''; position: absolute; inset: -6px; border-radius: 50%; border: 1.5px solid var(--color-accent-300); animation: playRing 2.2s ease-out infinite; }
  @keyframes playRing { 0% { transform: scale(.85); opacity: .9 } 100% { transform: scale(1.5); opacity: 0 } }

  /* transactions */
  .st-card { border-radius: 16px; background: var(--color-surface); box-shadow: 0 0 0 1px var(--color-neutral-800); overflow: hidden; }
  .st-card-h { display: flex; align-items: baseline; justify-content: space-between; padding: 14px 16px 6px; }
  .st-card-h a { font-size: 12px; color: var(--color-accent-300); text-decoration: none; }
  .tx { display: flex; align-items: center; gap: 12px; padding: 11px 16px; border-top: 1px solid var(--color-divider); }
  .tx:first-child { border-top: 0; }
  .tx { opacity: 0; transform: translateY(6px); animation: txIn .45s ease forwards; }
  .tx:nth-child(2) { animation-delay: .05s } .tx:nth-child(3) { animation-delay: .1s } .tx:nth-child(4) { animation-delay: .15s } .tx:nth-child(5) { animation-delay: .2s } .tx:nth-child(6) { animation-delay: .25s }
  @keyframes txIn { to { opacity: 1; transform: none } }
  .tx-ic { width: 32px; height: 32px; border-radius: 10px; flex: none; display: flex; align-items: center; justify-content: center; }
  .tx-ic.in { background: rgba(233,193,92,.14); color: #e9c15c; }
  .tx-ic.out { background: rgba(145,132,217,.14); color: var(--color-accent-300); }
  .tx-t { flex: 1; min-width: 0; }
  .tx-t b { display: block; font-weight: 500; font-size: 13px; line-height: 1.25; }
  .tx-t span { display: block; font-size: 11px; color: var(--color-neutral-500); margin-top: 1px; }
  .tx-a { text-align: right; font-variant-numeric: tabular-nums; }
  .tx-a b { display: block; font-weight: 500; font-size: 13px; }
  .tx-a.in b { color: #e9c15c; } .tx-a.out b { color: var(--color-accent-200); }
  .tx-a span { display: block; font-size: 10px; letter-spacing: .02em; color: var(--color-neutral-500); margin-top: 1px; line-height: 1.3; }
  .tx-a { flex: none; max-width: 96px; }

  /* mandate + manager */
  .st-row { display: flex; align-items: center; gap: 12px; padding: 14px 16px; cursor: pointer; }
  .st-row + .st-row { border-top: 1px solid var(--color-divider); }
  .st-row:hover { background: var(--color-neutral-900); }
  .st-ic { width: 36px; height: 36px; border-radius: 11px; flex: none; display: flex; align-items: center; justify-content: center; background: var(--color-neutral-900); color: var(--color-neutral-300); }
  .st-row .t { flex: 1; min-width: 0; } .st-row .t b { display: block; font-weight: 500; font-size: 14px; } .st-row .t span { display: block; font-size: 11px; color: var(--color-neutral-500); margin-top: 2px; }
  .st-row .r { font-size: 11px; color: var(--color-neutral-500); display: flex; align-items: center; gap: 8px; }
  .st-row .r i { color: var(--color-neutral-600); font-style: normal; }
  .ring { position: relative; width: 36px; height: 36px; flex: none; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 10px; color: var(--color-accent-200);
          background: conic-gradient(var(--color-accent) calc(var(--p) * 1%), var(--color-neutral-800) 0); }
  .ring::before { content: ''; position: absolute; inset: 3px; border-radius: 50%; background: var(--color-surface); }
  .ring span { position: relative; }
  .st-mgr { display: flex; align-items: center; gap: 12px; padding: 14px 16px; }
  .st-mgr .av { width: 40px; height: 40px; border-radius: 50%; flex: none; background: radial-gradient(circle at 35% 30%, #5b5f75, #2b2e3a); box-shadow: 0 0 0 1px var(--color-neutral-700);
                display: flex; align-items: center; justify-content: center; font-size: 14px; color: var(--color-neutral-200); }
  .st-mgr .t { flex: 1; min-width: 0; } .st-mgr .t b { display: block; font-weight: 500; font-size: 14px; } .st-mgr .t span { display: block; font-size: 11px; color: var(--color-neutral-500); margin-top: 2px; }
  .st-act { width: 36px; height: 36px; border-radius: 50%; border: 1px solid var(--color-accent-700); color: var(--color-accent-200); background: transparent; display: flex; align-items: center; justify-content: center; cursor: pointer; }
  .st-act:hover { background: rgba(145,132,217,.12); }
  .st-stats { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .st-stats > div { padding: 12px 14px; border-radius: 14px; background: var(--color-surface); box-shadow: 0 0 0 1px var(--color-neutral-800); }
  .st-stats span { display: block; font-size: 9px; letter-spacing: .14em; color: var(--color-neutral-400); }
  .st-stats b { display: block; font-weight: 500; font-size: 22px; letter-spacing: -.01em; margin-top: 4px; font-variant-numeric: tabular-nums; }
  .st-stats > div:nth-child(2) b { color: var(--color-accent-200); }
  .st-stats i { display: block; font-style: normal; font-size: 10px; color: var(--color-neutral-500); margin-top: 3px; }
  .tx-ic.lump { background: rgba(145,132,217,.22); color: var(--color-accent-100); }
  .tx-more[hidden] { display: none; }
  .tx-more .tx { animation-delay: 0s; }
  .fo { display: grid; grid-template-columns: 1fr auto; gap: 2px 12px; padding: 11px 16px; }
  .fo + .fo { border-top: 1px solid var(--color-divider); }
  .fo b { font-weight: 500; font-size: 13px; } .fo span { grid-row: 2; font-size: 10px; color: var(--color-neutral-500); letter-spacing: .04em; }
  .fo em { grid-row: 1 / span 2; align-self: center; font-style: normal; font-size: 13px; color: var(--color-neutral-200); font-variant-numeric: tabular-nums; }
  .kv { display: grid; grid-template-columns: 16px 1fr; gap: 12px; align-items: center; padding: 11px 16px; font-size: 12px; }
  .kv + .kv { border-top: 1px solid var(--color-divider); }
  .kv span { color: var(--color-neutral-500); display: flex; } .kv b { font-weight: 400; color: var(--color-neutral-200); word-break: break-word; line-height: 1.4; }
  .st-hero { position: relative; padding: 18px 18px 16px; border-radius: 18px; overflow: hidden; background: linear-gradient(160deg, #23263a 0%, #1b1d2d 55%, #161826 100%);
             box-shadow: 0 0 0 1px var(--color-neutral-800), 0 14px 30px -14px rgba(0,0,0,.7); }
  .st-hero::before { content: ''; position: absolute; right: -40px; top: -60px; width: 200px; height: 200px; border-radius: 50%; background: radial-gradient(circle, rgba(145,132,217,.22), transparent 70%); }
  .st-hero-k { font-size: 10px; letter-spacing: .14em; color: var(--color-neutral-400); }
  .st-hero-v { font-family: var(--font-heading); font-size: 36px; letter-spacing: -.02em; margin-top: 6px; font-variant-numeric: tabular-nums; }
  .st-hero-s { font-size: 11px; color: var(--color-neutral-500); margin-top: 4px; }
  .st-alloc { display: flex; height: 8px; border-radius: 4px; overflow: hidden; margin-top: 16px; gap: 2px; }
  .st-alloc i { display: block; height: 100%; border-radius: 2px; transform-origin: left; animation: growX .8s cubic-bezier(.2,.7,.2,1) both; }
  @keyframes growX { from { transform: scaleX(0) } }
  .st-legend { display: flex; flex-wrap: wrap; gap: 6px 14px; margin-top: 10px; font-size: 11px; color: var(--color-neutral-300); }
  .st-legend span { display: inline-flex; align-items: center; gap: 6px; } .st-legend s, .st-cal-f s, .fo s { text-decoration: none; display: inline-block; width: 8px; height: 8px; border-radius: 2px; }

  .st-cal { padding: 14px 16px 12px; border-radius: 16px; background: var(--color-surface); box-shadow: 0 0 0 1px var(--color-neutral-800); }
  .st-cal-h { display: flex; justify-content: space-between; align-items: baseline; } .st-cal-m { font-size: 11px; color: var(--color-neutral-500); }
  .st-cal-track { position: relative; height: 62px; margin: 22px 4px 0; }
  .st-cal-track::before { content: ''; position: absolute; left: 0; right: 0; top: 7px; height: 2px; background: linear-gradient(90deg, transparent, var(--color-neutral-700) 8%, var(--color-neutral-700) 92%, transparent); }
  .ev { position: absolute; top: 0; width: 0; }
  .ev i { position: absolute; left: -7px; top: 0; width: 14px; height: 14px; border-radius: 50%; background: var(--color-surface); border: 2px solid var(--color-accent); box-sizing: border-box; }
  .ev.done i { background: var(--color-accent); } .ev.in i { border-color: #e9c15c; } .ev.now i { border-color: var(--color-neutral-200); width: 10px; height: 10px; left: -5px; top: 2px; }
  .ev.now i::after { content: ''; position: absolute; inset: -8px; border-radius: 50%; border: 1px solid var(--color-neutral-400); animation: playRing 2s ease-out infinite; }
  .ev b, .ev span, .ev em { position: absolute; left: 0; transform: translateX(-50%); white-space: nowrap; text-align: center; font-style: normal; }
  .ev b { top: 22px; font-weight: 500; font-size: 11px; } .ev span { top: 37px; font-size: 10px; color: var(--color-neutral-500); } .ev em { top: 38px; font-size: 11px; color: var(--color-accent-200); font-variant-numeric: tabular-nums; }
  .ev.in em { color: #e9c15c; }
  .st-cal-f { display: flex; gap: 14px; margin-top: 8px; font-size: 10px; color: var(--color-neutral-500); } .st-cal-f span { display: inline-flex; align-items: center; gap: 6px; }
  .st-cal-f .dot { border-radius: 50%; } .st-cal-f .dot.done { background: var(--color-accent); } .st-cal-f .dot.in { background: #e9c15c; }

  .swp { display: flex; align-items: center; gap: 12px; padding: 14px 16px; } .swp + .swp { border-top: 1px solid var(--color-divider); }
  .swp-ic { width: 36px; height: 36px; border-radius: 11px; flex: none; display: flex; align-items: center; justify-content: center; }
  .swp-ic.gold { background: rgba(233,193,92,.14); color: #e9c15c; }
  .swp .t { flex: 1; min-width: 0; } .swp .t > b { display: block; font-weight: 500; font-size: 15px; } .swp .t > b small { font-size: 11px; font-weight: 400; color: var(--color-neutral-400); }
  .swp .t > span { display: block; font-size: 11px; color: var(--color-neutral-500); margin-top: 2px; }
  .swp-run { height: 4px; border-radius: 2px; background: var(--color-neutral-800); margin-top: 10px; overflow: hidden; } .swp-run i { display: block; height: 100%; background: #e9c15c; min-width: 3px; border-radius: 2px; }
  .swp-n { display: block; font-size: 10px; color: var(--color-neutral-500); margin-top: 5px; }

  .fo { display: grid; grid-template-columns: 8px 1fr auto; grid-template-rows: auto auto; gap: 4px 10px; align-items: center; padding: 12px 16px; }
  .fo + .fo { border-top: 1px solid var(--color-divider); }
  .fo s { grid-row: 1; border-radius: 50% !important; } .fo .t { grid-column: 2; min-width: 0; } .fo .a { grid-column: 3; text-align: right; font-variant-numeric: tabular-nums; }
  .fo .t b, .fo .a b { display: block; font-weight: 500; font-size: 13px; } .fo .t span, .fo .a span { display: block; font-size: 10px; color: var(--color-neutral-500); margin-top: 1px; letter-spacing: .02em; }
  .fo .bar { grid-column: 2 / 4; height: 3px; border-radius: 2px; background: var(--color-neutral-800); overflow: hidden; } .fo .bar i { display: block; height: 100%; min-width: 2px; border-radius: 2px; transform-origin: left; animation: growX .8s cubic-bezier(.2,.7,.2,1) both; }

  .st-upd { display: flex; align-items: center; gap: 12px; width: 100%; padding: 14px 16px; border-radius: 16px; border: 1px solid var(--color-accent-700); background: rgba(145,132,217,.08); color: var(--color-text); cursor: pointer; text-align: left; font: inherit; }
  .st-upd:hover { background: rgba(145,132,217,.14); }
  .st-upd-ic { width: 36px; height: 36px; border-radius: 11px; flex: none; display: flex; align-items: center; justify-content: center; background: var(--color-accent-800); color: var(--color-accent-100); }
  .st-upd.busy .st-upd-ic svg { animation: spin 1s linear infinite; } @keyframes spin { to { transform: rotate(360deg) } }
  .st-upd .t { flex: 1; min-width: 0; } .st-upd .t b { display: block; font-weight: 500; font-size: 14px; } .st-upd .t span { display: block; font-size: 11px; color: var(--color-neutral-400); margin-top: 2px; }
  .st-upd-arrow { color: var(--color-neutral-500); }
  .st-foot { text-align: center; font-size: 11px; color: var(--color-neutral-500); padding: 4px 0 10px; }
  .st-foot b { font-weight: 400; color: var(--color-neutral-400); }

  /* ---- screens ---- */
  .screen { flex: 1; min-height: 0; display: flex; flex-direction: column; }
  .screen[hidden] { display: none; }
  .hud[hidden] { display: none; }
  .pane { flex: 1; min-height: 0; overflow-y: auto; padding: 62px 22px var(--space-8); }

  .tier[hidden] { display: none; }
  .ph-navrow { display: flex; align-items: center; justify-content: space-between; gap: var(--space-4); }
  .ph-back { font-size: 13px; color: var(--color-accent-300); cursor: pointer; }
  .ph-more { margin-top: var(--space-6); text-align: center; font-size: 13px;
             color: var(--color-neutral-400); cursor: pointer; }
  .ph-more:hover, .ph-back:hover { color: var(--color-accent-200); }
  .ph-kicker { font-size: 10px; letter-spacing: .14em; color: var(--color-neutral-400); }
  .ph-hero { height: 190px; display: flex; align-items: center; justify-content: center;
             background: radial-gradient(circle at 50% 60%, rgba(108,186,54,.16) 0%, transparent 68%); }
  .ph-hero svg { width: 86%; height: 100%; display: block; }
  .ph-h1 { margin: var(--space-4) 0 0; font-family: var(--font-heading); font-weight: 500;
           font-size: 42px; letter-spacing: -.015em; text-align: center; }
  .ph-sub { margin-top: 8px; text-align: center; font-size: 15px; color: var(--color-neutral-300); }
  .ph-figrow { margin-top: var(--space-6); display: flex; align-items: baseline;
               justify-content: center; gap: var(--space-3); }
  .ph-big { font-family: var(--font-heading); font-size: 38px; letter-spacing: -.015em; }
  .ph-mid { font-family: var(--font-heading); font-size: 30px; letter-spacing: -.015em; }
  .ph-tag { font-size: 13px; color: var(--color-accent-300); white-space: nowrap; }
  .ph-lbl { font-size: 10px; letter-spacing: .12em; color: var(--color-neutral-400); }
  .ph-split { margin-top: var(--space-3); display: flex; justify-content: center; flex-wrap: wrap;
              gap: var(--space-3); font-size: 11px; color: var(--color-neutral-400); }
  .ph-split b { font-weight: 400; color: var(--color-neutral-300); }
  .ph-split i { color: var(--color-neutral-700); font-style: normal; }
  .acc { color: var(--color-accent-200); }
  .gold { color: #d9b463; }

  .ph-rail { margin-top: var(--space-8); display: flex; justify-content: space-between;
             font-size: 12px; color: var(--color-neutral-500);
             border-top: 1px solid var(--color-neutral-800); padding-top: var(--space-6); }
  .ph-rail .sel { color: var(--color-accent-200); }

  .ph-cost { margin-top: var(--space-6); padding: var(--space-4) var(--space-6);
             border-radius: var(--radius-md); background: var(--color-accent-800);
             font-size: 12px; color: var(--color-accent-200); }
  .ph-cost b { font-family: var(--font-heading); font-weight: 500; font-size: 20px;
               color: var(--color-accent-100); margin-right: 6px; }
  .ph-fine { margin-top: var(--space-6); font-size: 11px; line-height: 1.5; color: var(--color-neutral-500); }

  .ph-hl { margin-top: 8px; font-family: var(--font-heading); font-size: 24px;
           line-height: 1.25; letter-spacing: -.01em; }
  .ph-cards { display: flex; gap: var(--space-4); margin-top: var(--space-6); }
  .ph-card { flex: 1; padding: var(--space-4) var(--space-6); border-radius: var(--radius-md);
             background: var(--color-surface); box-shadow: var(--shadow-sm); }
  .ph-card-top { display: flex; align-items: center; gap: 7px; }
  .chip { width: 19px; height: 19px; flex: none; border-radius: 50%; display: flex;
          align-items: center; justify-content: center; font-size: 11px; }
  .chip.gold { background: linear-gradient(180deg,#ffe9a3,#f6c445 50%,#dc9a1f); color: #6b5111; }
  .chip.violet { background: var(--color-accent-800); color: var(--color-accent-100); }
  .ph-card-fig { margin-top: 6px; font-family: var(--font-heading); font-size: 26px;
                 letter-spacing: -.015em; font-variant-numeric: tabular-nums; }
  .ph-card-fig.gold { color: #f3dfa8; }
  .ph-bar { margin-top: 6px; height: 4px; border-radius: 2px; background: var(--color-neutral-900); overflow: hidden; }
  .ph-bar.tall { margin-top: var(--space-6); height: 10px; border-radius: 5px; background: #12141d; }
  .ph-bar i { display: block; height: 100%; border-radius: inherit; }
  .ph-card-sub { margin-top: 6px; font-size: 11px; color: var(--color-neutral-500); }

  .ph-sechd { margin-top: var(--space-8); display: flex; justify-content: space-between;
              align-items: baseline; font-size: 10px; letter-spacing: .12em; color: var(--color-neutral-400); }
  .ph-mut { font-size: 11px; letter-spacing: 0; color: var(--color-neutral-500); }
  .ph-tl { display: flex; margin-top: var(--space-6); }
  .ph-tl > div { flex: 1; position: relative; display: flex; flex-direction: column; align-items: center; }
  .ph-tl > div:not(:last-child)::after { content: ''; position: absolute; top: 11px; left: 50%;
    right: -50%; height: 1px; background: var(--color-divider); }
  .ph-tl .node { position: relative; z-index: 1; width: 22px; height: 22px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    background: var(--color-neutral-800); border: 1.5px solid var(--color-neutral-700); }
  .ph-tl .node i { font-style: normal; font-family: var(--font-heading); font-size: 12px; color: var(--color-neutral-300); }
  .ph-tl .node.now { background: linear-gradient(180deg,#ffe9a3,#f6c445 45%,#dc9a1f);
    border-color: #a86b12; box-shadow: 0 2px 5px rgba(0,0,0,.45); }
  .ph-tl .node.now i { color: #6b5111; }
  .ph-tl .m { margin-top: var(--space-4); font-size: 11px; letter-spacing: .06em; color: var(--color-neutral-400); }
  .ph-tl .a { margin-top: 3px; font-family: var(--font-heading); font-size: 13px; color: var(--color-neutral-300); }
  .ph-tl .on { color: var(--color-text); }

  .ph-vault { margin-top: var(--space-8); padding: var(--space-6); border-radius: var(--radius-md);
    background: linear-gradient(180deg,#262a38 0%,#1d2029 60%,#171a22 100%);
    box-shadow: inset 0 1px 0 rgba(255,255,255,.08), var(--shadow-sm); }
  .ph-vault-top { display: flex; align-items: center; justify-content: space-between; }
  .ph-vault-fig { margin-top: 4px; font-family: var(--font-heading); font-size: 38px;
    letter-spacing: -.02em; color: #f3dfa8; font-variant-numeric: tabular-nums; }
  .ph-vault-foot { display: flex; justify-content: space-between; margin-top: var(--space-2);
    font-size: 11px; color: var(--color-neutral-500); }

  .ph-call { margin-top: var(--space-8); display: flex; align-items: center; gap: var(--space-4);
    padding: var(--space-4) var(--space-6); border-radius: var(--radius-md);
    background: var(--color-surface); box-shadow: var(--shadow-sm); cursor: pointer; }
  .ph-callicon { width: 36px; height: 36px; flex: none; border-radius: 50%;
    background: var(--color-accent-900); display: flex; align-items: center; justify-content: center; }
  .ph-callbody { flex: 1; min-width: 0; }
  .ph-callbody b { display: block; font-weight: 400; font-size: 14px; }
  .ph-callbody i { display: block; font-style: normal; font-size: 11px;
    color: var(--color-neutral-500); margin-top: 2px; }

  /* ---- animations ---- */
  @keyframes shine { 0%,72% { transform: translateX(-140%) skewX(-18deg) }
                     100%  { transform: translateX(240%)  skewX(-18deg) } }
  @keyframes coinBob { 0%,100% { transform: translateY(0) } 50% { transform: translateY(-7px) } }
  @keyframes hammer  { 0%,100% { transform: rotate(-8deg) } 40% { transform: rotate(-72deg) } 62% { transform: rotate(6deg) } }
  @keyframes spark   { 0%,58% { opacity: 0; transform: scale(.4) } 64% { opacity: 1; transform: scale(1) }
                       78%,100% { opacity: 0; transform: scale(1.5) } }
  @keyframes ppFloat { 0%,100% { transform: translateY(0) } 50% { transform: translateY(-10px) } }
  @keyframes ppGlow { 0%,100% { opacity: .75 } 50% { opacity: 1 } }
  @keyframes glowPulse { 0%,100% { opacity: .75 } 50% { opacity: 1 } }
</style>